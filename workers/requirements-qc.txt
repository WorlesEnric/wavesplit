# QC worker (queue: auto-qc) — runs on its OWN host.
# Heavy dep isolated here: faster-whisper (ASR). NO torch / onnxruntime needed.
# Also requires the ffmpeg/ffprobe binaries on PATH (wavesplit.audio shells out).
requests>=2.31
PyYAML>=6
numpy>=1.26
rapidfuzz>=3.0
jiwer>=4.0
faster-whisper>=1.2
Unidecode>=1.4
